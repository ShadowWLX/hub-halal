# =============================================================================
# MODE RELEASE (PRODUCTION)
# =============================================================================
# Ce fichier est OPTIONNEL - la config est hardcodée dans le code
# Utilisé automatiquement par 'npm run build' et 'npm run dist'
#
# COMMANDES POUR LE MODE RELEASE:
# -------------------------------
# npm run build            → Build pour release (compile tout)
#                             API production + DevTools désactivés
#
# npm start                → Build release + lance l'app Electron
#                             (équivalent de l'ancien 'npm run start')
#                             Pas de DevTools, bouton Test Adhan caché
#
# npm run dist             → Build release + crée l'installateur Windows
#                             Génère les fichiers .exe dans le dossier dist/
#
# CARACTÉRISTIQUES DU MODE RELEASE:
# - DevTools DÉSACTIVÉS (pas de F12)
# - API Quran.Foundation PRODUCTION (officielle)
# - Bouton "Test Adhan" CACHÉ
# - Optimisé pour distribution finale
# =============================================================================

# Ces variables sont optionnelles - la config est dans src/renderer/config/api.ts
VITE_APP_ENV=release
VITE_ENABLE_TEST_FEATURES=false
