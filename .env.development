# =============================================================================
# MODE DÉVELOPPEMENT (DEV)
# =============================================================================
# Ce fichier est OPTIONNEL - la config est hardcodée dans le code
# Utilisé automatiquement par 'npm run dev'
#
# COMMANDES POUR LE MODE DEV:
# ---------------------------
# npm run dev              → Lance le serveur de développement (Vite + hot reload)
#                             Ouvre l'app avec DevTools activés
#                             API prelive + bouton Test Adhan visible
#
# npm run build:dev        → Build en mode dev (si besoin de tester le build dev)
#
# CARACTÉRISTIQUES DU MODE DEV:
# - DevTools accessibles (F12, Ctrl+Shift+I)
# - API Quran.Foundation PRELIVE (test)
# - Bouton "Test Adhan" visible
# - Hot reload pour développement rapide
# =============================================================================

# Ces variables sont optionnelles - la config est dans src/renderer/config/api.ts
VITE_APP_ENV=dev
VITE_ENABLE_TEST_FEATURES=true
